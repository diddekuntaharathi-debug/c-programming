#include <stdio.h>
int main (){
	int n,i,r[0];
	printf("Enter n value");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
	printf("\n enter value at r[%d] position",i);
	scanf("%d",&r[i]);
}
{
	printf("\n Array elements are:");
	}	
	for (i=0;i<=n;i++)
	{
		printf("%  d",r[i]);
	}
	return 0;
}
